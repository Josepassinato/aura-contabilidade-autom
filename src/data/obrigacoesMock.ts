
import { Obrigacao } from "@/types/obrigacoes";

// Este arquivo agora apenas exporta uma função para manter compatibilidade
// com código existente, mas os dados reais virão do Supabase
export const obrigacoesMock = [];

// Função mantida para compatibilidade com código existente
export const getObrigacoesWithCorrectTypes = (): Obrigacao[] => {
  // Importar do service real que busca do Supabase
  return [];
};
