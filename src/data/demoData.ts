// Arquivo removido - apenas dados reais do Supabase devem ser usados
export const demoClients: any[] = [];
export const demoDocuments: any[] = [];
export const demoFiscalEvents: any[] = [];
export const demoStats = {
  totalClients: 0,
  totalDocumentsPending: 0,
  totalUpcomingDeadlines: 0,
  fiscalSavings: 0,
  processingEfficiency: 0,
  clientSatisfaction: 0
};

export const demoFeatures: any[] = [];

// Funções mantidas para compatibilidade
export const initializeDemoData = () => {
  // Não faz nada - dados removidos
};

export const getDemoData = () => {
  return { clients: [], documents: [], events: [], stats: demoStats };
};

export const clearDemoData = () => {
  // Não faz nada - dados removidos
};